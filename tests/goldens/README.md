# Golden images

`compositions/` holds the reference frames for `tests/compositions.py`.

Regenerate only after you have inspected the new frames and agreed they are
the intended composition:

    python3 tests/compositions.py --write
